name: PR Preview

on:
  pull_request:
    types: [opened, reopened, synchronize, closed]

jobs:
  preview:
    uses: kristjanjansen/mfe-infra/.github/workflows/pr-preview.yml@main
    with:
      service_name: mfe-translations
      port: 4000
    secrets:
      infra_token: ${{ secrets.INFRA_TOKEN }}
